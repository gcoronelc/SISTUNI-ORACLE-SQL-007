WITH
V1 AS (
    SELECT IDDEPARTAMENTO, MAX(X.SUELDO) SUELDO_MAX
    FROM RECURSOS.EMPLEADO X
    GROUP BY IDDEPARTAMENTO
)
SELECT E.*
FROM RECURSOS.EMPLEADO E
WHERE E.SUELDO IN (SELECT SUELDO_MAX FROM V1)